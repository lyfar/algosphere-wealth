---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
---

<Layout title="Algosphere Asset Management">

  <!-- ═══════════════════════ NAVIGATION ═══════════════════════ -->
  <nav id="navbar" class="fixed top-0 left-0 right-0 z-50 transition-all duration-500 border-b border-transparent">
    <div class="max-w-7xl mx-auto px-6 lg:px-12 py-4 flex items-center justify-between">
      <!-- Logo -->
      <a href="#hero" class="flex items-center gap-3 group">
        <div class="logo-circle">
          <span class="text-gold-gradient font-heading text-xl font-semibold">A</span>
        </div>
        <span class="hidden sm:block text-sm tracking-[0.25em] uppercase text-gray-300 group-hover:text-white transition-colors duration-300 font-light">
          Algosphere
        </span>
      </a>

      <!-- Nav Links -->
      <div class="flex items-center gap-8 lg:gap-12">
        <a href="#about" class="nav-link text-xs tracking-[0.2em] uppercase text-gray-400 hover:text-gold-400 transition-colors duration-300">
          Ընկերություն
        </a>
        <a href="#vision" class="nav-link text-xs tracking-[0.2em] uppercase text-gray-400 hover:text-gold-400 transition-colors duration-300">
          Ծառայություններ
        </a>
        <a href="#contact" class="nav-link text-xs tracking-[0.2em] uppercase text-gray-400 hover:text-gold-400 transition-colors duration-300">
          Կապ
        </a>
      </div>
    </div>
  </nav>

  <!-- ═══════════════════════ HERO SECTION ═══════════════════════ -->
  <section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden noise-overlay">
    <!-- Starfield Background -->
    <div class="starfield" id="starfield"></div>
    <canvas id="particles-canvas"></canvas>
    
    <!-- Radial gradient overlay -->
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(212,175,55,0.04)_0%,transparent_70%)]"></div>
    
    <!-- Hero Content -->
    <div class="relative z-10 text-center px-6 max-w-4xl mx-auto">
      <!-- Logo Mark -->
      <div class="mb-10 flex justify-center" id="hero-logo">
        <div class="w-24 h-24 border border-gold-400/40 rounded-full flex items-center justify-center relative">
          <span class="text-gold-gradient font-heading text-5xl font-semibold">A</span>
          <div class="absolute inset-0 rounded-full border border-gold-400/10 scale-125 animate-pulse"></div>
        </div>
      </div>
      
      <!-- Title -->
      <h1 id="hero-title" class="font-heading text-4xl sm:text-5xl lg:text-6xl font-medium tracking-wide mb-2 opacity-0">
        <span class="text-white">ALGOSPHERE</span>
      </h1>
      <p id="hero-subtitle" class="text-xs sm:text-sm tracking-[0.5em] uppercase text-gold-400/80 mb-12 opacity-0 font-light">
        Asset Management
      </p>
      
      <!-- Tagline -->
      <p id="hero-tagline" class="text-lg sm:text-xl text-gray-400 font-light tracking-wide opacity-0">
        Navigating Markets with Intelligence
      </p>
      
      <!-- Decorative line -->
      <div id="hero-line" class="mt-12 mx-auto w-0 h-px bg-gradient-to-r from-transparent via-gold-400/40 to-transparent transition-all duration-1000"></div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-10 left-1/2 scroll-indicator opacity-0" id="scroll-indicator">
      <div class="w-6 h-10 border border-gold-400/30 rounded-full flex justify-center pt-2">
        <div class="w-1 h-2 bg-gold-400/50 rounded-full animate-bounce"></div>
      </div>
    </div>
  </section>

  <!-- Gold Divider -->
  <div class="gold-divider"></div>

  <!-- ═══════════════════════ ABOUT SECTION ═══════════════════════ -->
  <section id="about" class="relative py-32 lg:py-40">
    <div class="max-w-5xl mx-auto px-6 lg:px-12">
      <div class="reveal">
        <!-- Section Label -->
        <p class="text-xs tracking-[0.4em] uppercase text-gold-400/60 mb-6 font-light">About Us</p>
        
        <!-- Heading -->
        <h2 class="font-heading text-3xl sm:text-4xl lg:text-5xl text-white font-medium mb-12 leading-tight">
          Precision in Every<br />
          <span class="text-gold-gradient">Investment Decision</span>
        </h2>
      </div>
      
      <div class="reveal max-w-3xl" style="transition-delay: 0.2s;">
        <p class="text-lg text-gray-400 font-light leading-relaxed">
          Algosphere Asset Management specializes in managing closed, non-public investment funds, 
          providing innovative solutions. Our goal is to offer the most efficient investment strategies, 
          support sustainable financial growth, and build trusted partnerships.
        </p>
      </div>

      <!-- Abstract decorative element -->
      <div class="reveal mt-20 flex items-center gap-6" style="transition-delay: 0.4s;">
        <div class="h-px flex-1 bg-gradient-to-r from-gold-400/20 to-transparent"></div>
        <div class="w-2 h-2 border border-gold-400/40 rotate-45"></div>
        <div class="h-px w-24 bg-gold-400/20"></div>
      </div>
    </div>
  </section>

  <!-- Gold Divider -->
  <div class="gold-divider"></div>

  <!-- ═══════════════════════ VISION / MISSION / VALUES ═══════════════════════ -->
  <section id="vision" class="relative py-32 lg:py-40">
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
      <!-- Section Label -->
      <div class="reveal text-center mb-20">
        <p class="text-xs tracking-[0.4em] uppercase text-gold-400/60 mb-6 font-light">Our Principles</p>
        <h2 class="font-heading text-3xl sm:text-4xl lg:text-5xl text-white font-medium">
          Built on <span class="text-gold-gradient">Excellence</span>
        </h2>
      </div>

      <!-- Cards Grid -->
      <div class="grid md:grid-cols-3 gap-6 lg:gap-8">
        
        <!-- Vision Card -->
        <div class="reveal glass-card p-8 lg:p-10" style="transition-delay: 0.1s;">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 border border-gold-400/30 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-gold-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M2.036 12.322a1.012 1.012 0 010-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.638 0-8.573-3.007-9.963-7.178z" />
                <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>
            <h3 class="font-heading text-xl text-white font-medium">Vision</h3>
          </div>
          <p class="text-gray-400 font-light leading-relaxed text-sm">
            Creating long-term value for our investors through innovative and flexible solutions 
            while ensuring transparency, integrity, and regulatory compliance.
          </p>
        </div>

        <!-- Mission Card -->
        <div class="reveal glass-card p-8 lg:p-10" style="transition-delay: 0.25s;">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 border border-gold-400/30 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-gold-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 13.5l10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75z" />
              </svg>
            </div>
            <h3 class="font-heading text-xl text-white font-medium">Mission</h3>
          </div>
          <p class="text-gray-400 font-light leading-relaxed text-sm">
            To become Armenia's leading asset management company, recognized for extensive 
            experience in investment fund management and trusted by discerning investors.
          </p>
        </div>

        <!-- Values Card -->
        <div class="reveal glass-card p-8 lg:p-10" style="transition-delay: 0.4s;">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 border border-gold-400/30 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-gold-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z" />
              </svg>
            </div>
            <h3 class="font-heading text-xl text-white font-medium">Values</h3>
          </div>
          <div class="space-y-4">
            <div class="value-item">
              <p class="text-white text-sm font-medium mb-1">Integrity</p>
              <p class="text-gray-500 text-xs font-light">Unwavering commitment to ethical standards</p>
            </div>
            <div class="value-item">
              <p class="text-white text-sm font-medium mb-1">Innovation</p>
              <p class="text-gray-500 text-xs font-light">Forward-thinking investment strategies</p>
            </div>
            <div class="value-item">
              <p class="text-white text-sm font-medium mb-1">Client-Centricity</p>
              <p class="text-gray-500 text-xs font-light">Your success defines our purpose</p>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Gold Divider -->
  <div class="gold-divider"></div>

  <!-- ═══════════════════════ CONTACT / FOOTER ═══════════════════════ -->
  <footer id="contact" class="relative py-32 lg:py-40">
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
      <div class="grid lg:grid-cols-2 gap-20">
        
        <!-- Left: Logo & Info -->
        <div class="reveal">
          <div class="flex items-center gap-4 mb-8">
            <div class="logo-circle">
              <span class="text-gold-gradient font-heading text-xl font-semibold">A</span>
            </div>
            <div>
              <p class="text-white text-sm tracking-[0.2em] uppercase font-light">Algosphere</p>
              <p class="text-gold-400/60 text-xs tracking-[0.15em] uppercase">Asset Management</p>
            </div>
          </div>
          <p class="text-gray-500 text-sm font-light max-w-md leading-relaxed">
            Premium investment fund management with a commitment to excellence, 
            transparency, and superior returns.
          </p>
        </div>

        <!-- Right: Contact Details -->
        <div class="reveal" style="transition-delay: 0.2s;">
          <h3 class="font-heading text-2xl text-white font-medium mb-8">
            <span class="text-gold-gradient">Get in Touch</span>
          </h3>
          
          <div class="space-y-5">
            <!-- Address -->
            <div class="flex items-start gap-4">
              <svg class="w-5 h-5 text-gold-400/60 mt-0.5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z" />
              </svg>
              <p class="text-gray-400 text-sm font-light">Հայաստան, Երևան-0015, Արդարակի 7/6, 186</p>
            </div>

            <!-- Phone -->
            <div class="flex items-start gap-4">
              <svg class="w-5 h-5 text-gold-400/60 mt-0.5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 002.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 01-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 00-1.091-.852H4.5A2.25 2.25 0 002.25 4.5v2.25z" />
              </svg>
              <div class="text-gray-400 text-sm font-light">
                <a href="tel:+37411700013" class="hover:text-gold-400 transition-colors">+374 11 70 00 13</a>
                <br />
                <a href="tel:+37495700013" class="hover:text-gold-400 transition-colors">+374 95 70 00 13</a>
              </div>
            </div>

            <!-- Email -->
            <div class="flex items-start gap-4">
              <svg class="w-5 h-5 text-gold-400/60 mt-0.5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
              </svg>
              <a href="mailto:info@algosphere.am" class="text-gray-400 text-sm font-light hover:text-gold-400 transition-colors">
                info@algosphere.am
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Bottom Bar -->
      <div class="mt-20 pt-8 border-t border-white/5">
        <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
          <p class="text-gray-600 text-xs font-light tracking-wider">
            &copy; {new Date().getFullYear()} Algosphere Asset Management. All rights reserved.
          </p>
          <div class="flex items-center gap-2">
            <div class="w-1 h-1 bg-gold-400/30 rounded-full"></div>
            <p class="text-gray-600 text-xs font-light tracking-wider">
              Yerevan, Armenia
            </p>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <!-- ═══════════════════════ SCRIPTS ═══════════════════════ -->
  <script>
    // ─── Starfield Generator ───
    function createStarfield() {
      const container = document.getElementById('starfield');
      if (!container) return;
      
      const starCount = 120;
      for (let i = 0; i < starCount; i++) {
        const star = document.createElement('div');
        star.className = 'star';
        star.style.left = `${Math.random() * 100}%`;
        star.style.top = `${Math.random() * 100}%`;
        star.style.setProperty('--duration', `${2 + Math.random() * 5}s`);
        star.style.setProperty('--max-opacity', `${0.3 + Math.random() * 0.7}`);
        star.style.animationDelay = `${Math.random() * 5}s`;
        star.style.width = `${1 + Math.random() * 2}px`;
        star.style.height = star.style.width;
        container.appendChild(star);
      }
    }

    // ─── Particle Canvas ───
    function initParticles() {
      const canvas = document.getElementById('particles-canvas') as HTMLCanvasElement;
      if (!canvas) return;
      const ctx = canvas.getContext('2d');
      if (!ctx) return;

      let w = canvas.width = canvas.offsetWidth;
      let h = canvas.height = canvas.offsetHeight;

      interface Particle {
        x: number; y: number; vx: number; vy: number; r: number; alpha: number;
      }

      const particles: Particle[] = [];
      const count = 50;

      for (let i = 0; i < count; i++) {
        particles.push({
          x: Math.random() * w,
          y: Math.random() * h,
          vx: (Math.random() - 0.5) * 0.3,
          vy: (Math.random() - 0.5) * 0.3,
          r: Math.random() * 1.5 + 0.5,
          alpha: Math.random() * 0.5 + 0.1,
        });
      }

      function draw() {
        ctx!.clearRect(0, 0, w, h);
        
        // Draw connections
        for (let i = 0; i < particles.length; i++) {
          for (let j = i + 1; j < particles.length; j++) {
            const dx = particles[i].x - particles[j].x;
            const dy = particles[i].y - particles[j].y;
            const dist = Math.sqrt(dx * dx + dy * dy);
            if (dist < 150) {
              ctx!.beginPath();
              ctx!.strokeStyle = `rgba(212, 175, 55, ${0.05 * (1 - dist / 150)})`;
              ctx!.lineWidth = 0.5;
              ctx!.moveTo(particles[i].x, particles[i].y);
              ctx!.lineTo(particles[j].x, particles[j].y);
              ctx!.stroke();
            }
          }
        }

        // Draw particles
        for (const p of particles) {
          ctx!.beginPath();
          ctx!.arc(p.x, p.y, p.r, 0, Math.PI * 2);
          ctx!.fillStyle = `rgba(212, 175, 55, ${p.alpha})`;
          ctx!.fill();

          p.x += p.vx;
          p.y += p.vy;

          if (p.x < 0 || p.x > w) p.vx *= -1;
          if (p.y < 0 || p.y > h) p.vy *= -1;
        }

        requestAnimationFrame(draw);
      }

      draw();

      window.addEventListener('resize', () => {
        w = canvas.width = canvas.offsetWidth;
        h = canvas.height = canvas.offsetHeight;
      });
    }

    // ─── Navbar scroll effect ───
    function initNavbar() {
      const navbar = document.getElementById('navbar');
      if (!navbar) return;

      window.addEventListener('scroll', () => {
        if (window.scrollY > 80) {
          navbar.classList.add('nav-solid');
        } else {
          navbar.classList.remove('nav-solid');
        }
      });
    }

    // ─── Hero entrance animations ───
    function animateHero() {
      const elements = [
        { id: 'hero-logo', delay: 300 },
        { id: 'hero-title', delay: 600 },
        { id: 'hero-subtitle', delay: 900 },
        { id: 'hero-tagline', delay: 1200 },
        { id: 'hero-line', delay: 1500 },
        { id: 'scroll-indicator', delay: 2000 },
      ];

      elements.forEach(({ id, delay }) => {
        const el = document.getElementById(id);
        if (!el) return;
        setTimeout(() => {
          el.style.transition = 'opacity 1s cubic-bezier(0.23, 1, 0.32, 1), transform 1s cubic-bezier(0.23, 1, 0.32, 1)';
          el.style.opacity = '1';
          el.style.transform = 'translateY(0)';
          if (id === 'hero-line') {
            el.style.width = '120px';
          }
        }, delay);
      });

      // Set initial transforms
      ['hero-title', 'hero-subtitle', 'hero-tagline'].forEach(id => {
        const el = document.getElementById(id);
        if (el) el.style.transform = 'translateY(20px)';
      });
    }

    // ─── Scroll reveal ───
    function initScrollReveal() {
      const reveals = document.querySelectorAll('.reveal');

      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('active');
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.15, rootMargin: '0px 0px -50px 0px' }
      );

      reveals.forEach((el) => observer.observe(el));
    }

    // ─── Smooth scroll for nav links ───
    function initSmoothScroll() {
      document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', (e) => {
          e.preventDefault();
          const target = document.querySelector((anchor as HTMLAnchorElement).getAttribute('href')!);
          if (target) {
            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
          }
        });
      });
    }

    // ─── Parallax on hero ───
    function initParallax() {
      const heroContent = document.querySelector('#hero > .relative.z-10') as HTMLElement;
      if (!heroContent) return;

      window.addEventListener('scroll', () => {
        const scrolled = window.scrollY;
        if (scrolled < window.innerHeight) {
          heroContent.style.transform = `translateY(${scrolled * 0.3}px)`;
          heroContent.style.opacity = `${1 - scrolled / (window.innerHeight * 0.8)}`;
        }
      });
    }

    // Init everything
    document.addEventListener('DOMContentLoaded', () => {
      createStarfield();
      initParticles();
      initNavbar();
      animateHero();
      initScrollReveal();
      initSmoothScroll();
      initParallax();
    });
  </script>
</Layout>
