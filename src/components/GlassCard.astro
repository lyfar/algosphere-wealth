---
/**
 * Reusable glass card with icon, title, and body content.
 */
interface Props {
  title: string;
  titleHy?: string;
  titleRu?: string;
  titleFr?: string;
  titleAr?: string;
  iconPath: string;
  /** Additional SVG path (some icons have 2) */
  iconPath2?: string;
  class?: string;
}

const { title, titleHy, titleRu, titleFr, titleAr, iconPath, iconPath2, class: className = '' } = Astro.props;
---

<div class:list={['glass-card p-10 lg:p-12 gs-reveal-scale', className]}>
  <div class="mb-8">
    <div class="w-14 h-14 border border-glass-border rounded-2xl flex items-center justify-center bg-gold-400/[0.06] mb-5">
      <svg class="w-7 h-7 text-gold-accent" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
        <path stroke-linecap="round" stroke-linejoin="round" d={iconPath} />
        {iconPath2 && <path stroke-linecap="round" stroke-linejoin="round" d={iconPath2} />}
      </svg>
    </div>
    <h3
      class="font-heading text-xl lg:text-[1.35rem] text-card-title font-semibold leading-tight"
      data-i18n
      data-hy={titleHy || title}
      data-en={title}
      data-ru={titleRu || title}
      data-fr={titleFr || title}
      data-ar={titleAr || title}
    >
      {titleHy || title}
    </h3>
  </div>
  <slot />
</div>
