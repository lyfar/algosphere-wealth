---
/**
 * Gold gradient divider line — Armenian-inspired ornate divider.
 * Phase 3: Added center diamond motif inspired by khachkar geometry.
 */
interface Props {
  /** Use the khachkar-style divider variant */
  ornate?: boolean;
}

const { ornate = false } = Astro.props;
---

{ornate ? (
  <div class="khachkar-divider gs-line-h">
    <div class="khachkar-divider-center"></div>
  </div>
) : (
  <div class="gold-divider gs-line-h"></div>
)}
